document.addEventListener('input',(e)=>{if(e.target.id==='resourceSearch'||e.target.id==='resourceType'){const q=(document.getElementById('resourceSearch')?.value||'').toLowerCase();const type=document.getElementById('resourceType')?.value||'';document.querySelectorAll('#resourcesGrid .resource-card').forEach(card=>{const okQ=card.dataset.search.includes(q);const okT=!type||card.dataset.type===type;card.style.display=(okQ&&okT)?'block':'none';});}});